import { RingLoader } from "react-spinners";

const RouteSpinner = () => (
  <div className="flex min-h-[50vh] items-center justify-center">
    <RingLoader color="#e2136e" />
  </div>
);

export default RouteSpinner;
